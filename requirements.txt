# Core
streamlit>=1.40
anthropic>=0.40
pypdf>=5.0
pyyaml>=6.0
numpy>=1.26

# Retrieval + verification
sentence-transformers>=3.0
transformers>=4.44
torch>=2.2
# DeBERTa-v3 ships a SentencePiece vocab with no pre-built tokenizer.json, so
# the tokenizer cannot be constructed without these two. Omit them and the NLI
# verifier fails to load at runtime rather than at install time.
sentencepiece>=0.2
protobuf>=4.0

# Optional: exact-search acceleration. The retriever falls back to an exact
# numpy dot product if this is absent, which is fine at our corpus size.
faiss-cpu>=1.8; platform_system != "Darwin" or platform_machine != "arm64"

# Optional: voice input. Omit to run text-only.
faster-whisper>=1.0
